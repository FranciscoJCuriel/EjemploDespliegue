apiVersion: v1
kind: Service
metadata:
  name: pruebadespliegue
  namespace: namespace-workflow-1698304675830
spec:
  type: LoadBalancer
  selector:
    app: pruebadespliegue
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000