apiVersion: v1
kind: Service
metadata:
  name: despliegue13
  namespace: namespace-workflow-1698582457859
spec:
  type: LoadBalancer
  selector:
    app: despliegue13
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000