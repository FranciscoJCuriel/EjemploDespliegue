apiVersion: v1
kind: Service
metadata:
  name: despiegue11
  namespace: namespace-workflow-1698561074892
spec:
  type: LoadBalancer
  selector:
    app: despiegue11
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000