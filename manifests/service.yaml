apiVersion: v1
kind: Service
metadata:
  name: despliegue5
  namespace: namespace-workflow-1698310993509
spec:
  type: LoadBalancer
  selector:
    app: despliegue5
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000