apiVersion: v1
kind: Service
metadata:
  name: despliegue8
  namespace: namespace-workflow-1698317290442
spec:
  type: LoadBalancer
  selector:
    app: despliegue8
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000