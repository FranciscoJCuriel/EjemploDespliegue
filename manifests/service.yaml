apiVersion: v1
kind: Service
metadata:
  name: despliegue13
  namespace: namespace-workflow-1698583336555
spec:
  type: LoadBalancer
  selector:
    app: despliegue13
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000