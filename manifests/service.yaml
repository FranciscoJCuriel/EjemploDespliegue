apiVersion: v1
kind: Service
metadata:
  name: despliegue10
  namespace: namespace-workflow-1698554353279
spec:
  type: LoadBalancer
  selector:
    app: despliegue10
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000