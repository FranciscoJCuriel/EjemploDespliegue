apiVersion: v1
kind: Service
metadata:
  name: pruebadespliegue
  namespace: namespace-workflow-1698305680252
spec:
  type: LoadBalancer
  selector:
    app: pruebadespliegue
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000