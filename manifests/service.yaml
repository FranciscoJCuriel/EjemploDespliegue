apiVersion: v1
kind: Service
metadata:
  name: despliegue9
  namespace: namespace-workflow-1698527924670
spec:
  type: LoadBalancer
  selector:
    app: despliegue9
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000