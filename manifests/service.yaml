apiVersion: v1
kind: Service
metadata:
  name: despliegue7
  namespace: namespace-workflow-1698316562600
spec:
  type: LoadBalancer
  selector:
    app: despliegue7
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000